import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import './mp.css';

/* ─── Контракт с оболочкой ────────────────────────────────────────────────
   core/shell.js забирает mp.html/.css/.js как текст, вставляет html+css в
   контейнер (создаётся ОДИН раз и дальше только скрывается через display),
   а js исполняет как инлайновый <script>. Скрипт выполняется ровно один раз,
   при первом открытии модуля, поэтому React-корень создаётся здесь и живёт
   всю сессию: переключение вкладок оболочки только прячет контейнер.

   Модуль значится в таблице WS-диспетчеризации shell.js — оболочка шлёт сюда
   window.MP.onWS(data). Серверной синхронизации пока нет, но точка входа
   должна существовать, иначе оболочка молча ничего не найдёт. */

window.MP = {
  onWS() { /* серверная синхронизация — следующий этап */ },
};

const el = document.getElementById('mp-mount');
if (el) createRoot(el).render(<App />);
